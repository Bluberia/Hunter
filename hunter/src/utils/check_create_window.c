/*
** EPITECH PROJECT, 2018
** tetris
** File description:
** check at start of the hunter
*/

#include <stdlib.h>
#include "proto.h"

int check_create_window(sfRenderWindow* window)
{
	if (window == NULL)
		return (ERROR);
	return (OK);
}
